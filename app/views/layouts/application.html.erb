<!DOCTYPE html>
<html>
  <head>
    <title>Ratebeer</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <div class="navibar">
    <%= link_to 'breweries', breweries_path %>
    <%= link_to 'beers', beers_path %>
    <%= link_to 'ratings', ratings_path%>
    <%= link_to 'users', users_path %>
    <%= link_to 'beer clubs', beer_clubs_path %>
    <%= link_to 'places', places_path %>
    <%= link_to 'styles', styles_path %>
    |
    <% if current_user %>
      <%= link_to current_user.username, user_path(id: current_user.id) %>
      <%= link_to 'rate a new beer', new_rating_path %>
      <%= link_to 'join a new beer club', new_membership_path %>
      <%= link_to 'sign out', signout_path, method: :delete, data: {"turbo-method": :delete}, class: "link-secondary smooth" %>
    <% else %>
      <%= link_to 'sign in', signin_path %>
      <%= link_to 'sign up', new_user_path %>
    <% end %>
    </div>

    <% if current_user %>
      <p><%= current_user.username %> signed in</p>
    <% end %>

    <%= yield %>
  </body>
</html>
